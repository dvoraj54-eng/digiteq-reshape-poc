# Android test app (Tips app)

Empty on purpose – Claude Code creates the Android Studio project here (`android/TipsApp/`) following
`docs/android-app-spec.md`. Setup of the Mac: `docs/android-setup-mac.md`.
